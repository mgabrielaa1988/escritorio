
/** \brief Solicita un numero al usuario
 * \param Valor numerico del tipo float
 * \return Valor numerico de tipo float ingresado por el usuario
 */
float pedirNumero();

/** \brief Toma el valor ingresado por el usuario como radio de un circulo y calcula la circunferencia
 * \param Valor numerico del tipo float que representa el radio de un circulo
 * \return Valor numerico de tipo float correspondiente al area del circulo
 */
float areaCirculo(float radio);
